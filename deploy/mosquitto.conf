# Both listeners stay on loopback; nginx is the only thing exposed publicly
# (see deploy/nginx.conf.template), matching the loopback-only posture the
# rest of this project already uses for local-only services.
listener 1883 127.0.0.1
allow_anonymous true

listener 9001 127.0.0.1
protocol websockets
allow_anonymous true
